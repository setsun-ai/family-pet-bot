# LF everywhere (the bot runs on Linux / Raspberry Pi too)...
* text=auto eol=lf
# ...except Windows scripts: cmd.exe mis-handles labels in LF-only .bat files.
*.bat text eol=crlf
*.ps1 text eol=crlf
